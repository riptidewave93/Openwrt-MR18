image: bradrydzewski/base
script:
  - sudo apt-get update
  - sudo apt-get install -y git-core build-essential libssl-dev libncurses5-dev unzip subversion mercurial libncurses5-dev zlib1g-dev gawk
  - chmod +x ./build.sh
  - ./build.sh
notify:
  email:
    recipients:
      - chrisrblake93@gmail.com
